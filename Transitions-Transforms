/* Button Transitions */

.button {
color: white;
background: blue;
transition-duration: .4s;
transition-property: all;
transition-property: background;
}

.button:hover {
background: #d36a62;
border-radius: 1em;

.button:active {
background: #a33830;
}

.button {
color: red;
border: 2x solid;
transition-property: background, border-radius, color;
transition-duration: .4s, .8s, .4s;
}

.button:hover {
color: #fff;
background: red;
border-radius: 1em;
}

/* Img Transitions */

.img:hover {
opacity: 0;
transition-duration: .4s;
}

.photo-overlay {
opacity: 0;
transition-property: opacity;
transition-duration: 1s;
}

.photo-overlay:hover {
opacity: 1;
}

/* */

img {
transition: box-shadow .6s;
}

img:hover {
box-shadow: 0 0 25px reba (0,0,0, .6);
}

/* Input Field Selection */

.form-input {
border: 3px solid;
border-color: #f0f0f0;
transition: border-color .6s;
}

.form-input:focus {
border-color: #52bab3;
}
